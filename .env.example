# ==============================================================================
# PLAYER CONNECT - ENVIRONNEMENT VARIABLES
# ==============================================================================
# Copiez ce fichier vers .env.local pour le développement
# ou configurez ces variables dans votre plateforme de déploiement (Vercel, etc.)
# ==============================================================================

# ------------------------------------------------------------------------------
# Base de données PostgreSQL (REQUIS)
# ------------------------------------------------------------------------------
# URL de connexion à la base de données PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/playerconnect?schema=public"

# URL de connexion directe (pour Prisma Accelerate ou connexions poolées)
DIRECT_URL="postgresql://user:password@localhost:5432/playerconnect?schema=public"

# ------------------------------------------------------------------------------
# Better Auth - Authentification (REQUIS)
# ------------------------------------------------------------------------------
# Secret pour signer les tokens de session (générez avec: openssl rand -base64 32)
BETTER_AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# URL publique de votre application
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ------------------------------------------------------------------------------
# OAuth Google (REQUIS pour la connexion Google)
# ------------------------------------------------------------------------------
# Obtenez ces clés sur: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ------------------------------------------------------------------------------
# OpenAI API (OPTIONNEL - pour le parsing de stats)
# ------------------------------------------------------------------------------
# Clé API OpenAI pour l'analyse automatique des statistiques
OPENAI_API_KEY="sk-..."

# ------------------------------------------------------------------------------
# Pusher - Messagerie en temps réel (OPTIONNEL)
# ------------------------------------------------------------------------------
# Configuration Pusher pour le chat en temps réel
# Obtenez ces clés sur: https://pusher.com
NEXT_PUBLIC_PUSHER_APP_ID="your-pusher-app-id"
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"
NEXT_PUBLIC_PUSHER_SECRET="your-pusher-secret"
NEXT_PUBLIC_PUSHER_CLUSTER="eu"

# ------------------------------------------------------------------------------
# Supabase - Alternative à Pusher (OPTIONNEL)
# ------------------------------------------------------------------------------
# Configuration Supabase si vous utilisez leurs services
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# ------------------------------------------------------------------------------
# GitHub (OPTIONNEL - pour afficher les stars sur la page d'accueil)
# ------------------------------------------------------------------------------
# Token OAuth GitHub pour afficher le nombre de stars
GITHUB_OAUTH_TOKEN="ghp_..."

# ------------------------------------------------------------------------------
# Email - Resend (OPTIONNEL - pour l'envoi d'emails)
# ------------------------------------------------------------------------------
# Clé API Resend pour l'envoi d'emails
RESEND_API_KEY="re_..."

# ------------------------------------------------------------------------------
# Stripe - Paiements (OPTIONNEL - si abonnements activés)
# ------------------------------------------------------------------------------
# Clés API Stripe pour gérer les paiements
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# ------------------------------------------------------------------------------
# Environnement
# ------------------------------------------------------------------------------
# Mode: development | production | test
NODE_ENV="development"

# ==============================================================================
# NOTES IMPORTANTES
# ==============================================================================
#
# 🔴 VARIABLES REQUISES POUR PRODUCTION:
#   - DATABASE_URL
#   - DIRECT_URL (si différent de DATABASE_URL)
#   - BETTER_AUTH_SECRET
#   - NEXT_PUBLIC_APP_URL
#   - GOOGLE_CLIENT_ID
#   - GOOGLE_CLIENT_SECRET
#
# 🟡 VARIABLES OPTIONNELLES (fonctionnalités limitées sans elles):
#   - OPENAI_API_KEY (parsing automatique des stats désactivé)
#   - PUSHER_* (chat en temps réel désactivé)
#   - SUPABASE_* (fonctionnalités Supabase désactivées)
#   - STRIPE_* (paiements désactivés)
#
# 🔒 SÉCURITÉ:
#   - Ne commitez JAMAIS le fichier .env ou .env.local
#   - Utilisez des secrets forts (minimum 32 caractères aléatoires)
#   - Changez BETTER_AUTH_SECRET en production
#   - Activez la restriction d'origine pour Google OAuth en production
#
# 📝 CONFIGURATION OAUTH GOOGLE:
#   1. Créez un projet sur https://console.cloud.google.com
#   2. Activez l'API Google+ ou People API
#   3. Créez des identifiants OAuth 2.0
#   4. Ajoutez les URIs de redirection autorisées:
#      - http://localhost:3000/api/auth/callback/google (dev)
#      - https://votre-domaine.com/api/auth/callback/google (prod)
#
# ==============================================================================
