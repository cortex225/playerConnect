# ============================================================================
# VARIABLES À CONFIGURER SUR VERCEL - URGENT
# ============================================================================
# Copiez ces variables dans Vercel Dashboard → Settings → Environment Variables
# ============================================================================

# ----------------------------------------------------------------------------
# 1. BASE DE DONNÉES SUPABASE (OBLIGATOIRE)
# ----------------------------------------------------------------------------
#
# Où trouver ces URLs :
# → Supabase Dashboard → Settings → Database → Connection string
#
# Pour DATABASE_URL : Sélectionnez "Session pooling" puis "Connection string"
DATABASE_URL=postgresql://postgres.xxxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:5432/postgres?pgbouncer=true

# Pour DIRECT_URL : Sélectionnez "Direct connection" puis "Connection string"
DIRECT_URL=postgresql://postgres.xxxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:5432/postgres

# ⚠️ IMPORTANT :
# - DATABASE_URL DOIT avoir ?pgbouncer=true à la fin
# - DIRECT_URL NE DOIT PAS avoir ?pgbouncer=true
# - Remplacez [YOUR-PASSWORD] par votre vrai mot de passe Supabase
# - Si vous ne connaissez pas le mot de passe : Supabase → Settings → Database → Reset database password

# ----------------------------------------------------------------------------
# 2. BETTER AUTH (OBLIGATOIRE)
# ----------------------------------------------------------------------------
#
# Générez un secret sécurisé avec cette commande dans votre terminal :
# openssl rand -base64 32
#
BETTER_AUTH_SECRET=COLLEZ_ICI_VOTRE_SECRET_GENERE_32_CARACTERES_MINIMUM

# URL de votre application Vercel (remplacez par votre vraie URL)
NEXT_PUBLIC_APP_URL=https://votre-projet.vercel.app

# ----------------------------------------------------------------------------
# 3. GOOGLE OAUTH (OBLIGATOIRE pour connexion Google)
# ----------------------------------------------------------------------------
#
# Où trouver ces valeurs :
# → Google Cloud Console → APIs & Services → Credentials
# → https://console.cloud.google.com/apis/credentials
#
GOOGLE_CLIENT_ID=xxxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxx

# ⚠️ IMPORTANT : Configurez aussi l'URI de redirection OAuth dans Google Cloud Console :
# → Credentials → Votre OAuth 2.0 Client → Authorized redirect URIs
# → Ajoutez : https://votre-projet.vercel.app/api/auth/callback/google

# ============================================================================
# COMMENT AJOUTER CES VARIABLES SUR VERCEL
# ============================================================================
#
# Méthode 1 - Interface Web (recommandé) :
# 1. Allez sur https://vercel.com/dashboard
# 2. Sélectionnez votre projet "playerConnect"
# 3. Settings → Environment Variables
# 4. Cliquez "Add New"
# 5. Pour chaque variable ci-dessus :
#    - Key : Le nom (ex: DATABASE_URL)
#    - Value : La valeur (ex: postgresql://...)
#    - Environments : Cochez Production, Preview, Development
# 6. Cliquez "Save"
# 7. Répétez pour les 6 variables
#
# Méthode 2 - CLI Vercel :
# 1. Installez : npm i -g vercel
# 2. Login : vercel login
# 3. Ajoutez : vercel env add DATABASE_URL production
# 4. Collez la valeur quand demandé
# 5. Répétez pour les autres variables
#
# ============================================================================
# APRÈS AVOIR AJOUTÉ LES VARIABLES
# ============================================================================
#
# Redéployez sur Vercel :
# → Vercel Dashboard → Deployments → Dernière build → ⋯ → Redeploy
#
# ============================================================================
